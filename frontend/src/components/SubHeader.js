const SubHeader = ({ title }) => {
  return (
    <>
      <header className="subheader">
        <div className="container">
          <h1>{title}</h1>
        </div>
      </header>
    </>
  );
};

export default SubHeader;
